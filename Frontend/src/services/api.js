const API_BASE_URL = "http://localhost:8080/api";

// Future: GET /sensor-data?page=...&size=20&search=...&from=...&to=...&sortBy=...&order=...
// Future: getActionHistory(params)
// Future: GET /profile and PUT or PATCH /profile with JWT.
// Avatar upload/storage remains an implementation detail; production persists avatarUrl.
// Replace mock data calls with Spring Boot API calls here later.
export { API_BASE_URL };
